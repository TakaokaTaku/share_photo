<% provide(:title, '通知') %>
<% notices = @notices.where.not(visitor_id: current_user.id) %>

<% if notices.any? %>
  <ul class="users">
    <%= render notices %>
  </ul>
  <%= will_paginate %>
<% else %>
  通知はありません
<% end %>
