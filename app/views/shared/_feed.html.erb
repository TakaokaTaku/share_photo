<% if feed_items.any? %>
  <div class="posts">
    <%= render feed_items %>
  </div>
  <div class="pagination-box">
    <%= will_paginate feed_items,
                      params: { controller: :static_pages, action: :home } %>
  </div>
<% end %>
