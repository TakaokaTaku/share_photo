<div class="post-data">
  <span>
    お気に入り: <strong><%= post.likers.count %></strong>
  </span>
  <span>
    コメント:<strong><%= post.comments.count %></strong>
  </span>
  <span>
    <%= time_ago_in_words(post.created_at) %>前
  </span>
</div>
