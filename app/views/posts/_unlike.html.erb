<%= form_with(model: current_user.active_favorites.find_by(liked_id: @post.id),
               html: { method: :delete }, local: false) do |f| %>
  <%= f.submit "お気に入り解除", class: "btn btn-lg" %>
<% end %>
