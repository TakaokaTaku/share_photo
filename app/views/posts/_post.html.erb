<button class="btn garally" id="post-<%= post.id %>" data-toggle = "modal" data-target = "#Modal-<%= post.id %>">
  <%= image_tag post.display_image if post.image.attached? %>
</button>
<%= render 'posts/post_modal',post: post, user: post.user %>
